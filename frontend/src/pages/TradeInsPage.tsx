import { Typography, Box } from '@mui/material';

export default function TradeInsPage() {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Canjes
      </Typography>
      <Typography variant="body1">
        Gestión de canjes de propiedades
      </Typography>
    </Box>
  );
}
